# Yansilu 关联图谱收口清单

更新时间：2026-06-01

## 已收口

- 关联笔记面板恢复可操作：候选笔记可选、关系类型可选、显式插入按钮可用。
- 关联来源与关系类型文案统一，临时关系卡与正式关系卡来源词一致。
- 图谱默认视角收敛为“论证图 / 结构图”，并记住最近一次筛选偏好。
- 图谱总览补回收口区块：图谱读法、图谱洞见、桥接缺口、待补关系理由、AI 图谱初判。
- 孤立笔记和断裂簇重新获得稳定入口：`去补桥接` 直接进入桥接关系表单。
- 图谱 follow-up 表单支持模板切换、偏好记忆、偏好清除和覆盖确认。
- 图谱刷新支持超时、保留旧图、就地重试。

## 当前验证

### 自动回归

命令：

```powershell
npm.cmd run test:e2e:browser:relations-graph-closeout
```

当前通过项：

- bridge gap follow-up 打开关系创建表单
- tension follow-up 打开边界字段
- relation 模板偏好刷新后恢复
- relation / distillation 模板偏好可清除

### 真页核对

目标页：

- `http://127.0.0.1:5175/prototype`

已确认：

- 图谱总览能显示 `桥接缺口 / 待补关系理由 / AI 图谱初判`
- `去补桥接` 能打开桥接关系表单
- 表单会预填：
  - `relationType = bridges`
  - 默认模板 = `写作版`
  - 图谱进入提示文案
- 图谱总览的统计口径说明已补清：
  - 顶部摘要显示当前筛选后的可见关系
  - 读法卡说明“下面这组数字按当前目录的全部结构统计”
- 控制台 `error / warn` 为空

## 剩余待办

### 已澄清

- `http://127.0.0.1:5175/prototype` 与 `prototype.html` 共享同一份页面壳，不是两套前端。
- 当前页面壳的契约是：
  - 有 `graph` rail
  - 没有 `explorer` rail 按钮
  - 状态节点是 `#statusBar / #statusText`，不是 `#statusLine`
- 已补专门回归，避免后续图谱测试再次误用 `explorer/statusLine` 假设。

### P2

- 给图谱总览补一张稳定截图产物
  - 当前 in-app browser 的 `Page.captureScreenshot` 在这页偶发超时
  - 如果后续需要 PR/验收材料，优先用本地 Playwright 产图

### P2

- 继续压一轮密集目录的阅读噪音
  - 候选方向：把“桥接缺口 / 待补关系理由 / AI 初判”改成可折叠 section
  - 候选方向：在总览态进一步降低非焦点标题露出数量

## 建议作为本轮收尾口径

- 功能主链已闭环
- closeout 自动回归已落地并可重复执行
- 真页关键入口已核对
- 剩余项主要是页面壳一致性与验收材料补齐，不再是主功能阻塞
